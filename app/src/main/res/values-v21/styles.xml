<resources>

    <style name="harf_transparents">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    </style>

    <!-- inherit from the material theme -->
    <style name="AppTheme" parent="android:Theme.Material">
        <!-- Main theme colors -->
        <!--   your app's branding color (for the app bar) -->
        <item name="android:colorPrimary">@color/end_color</item>
        <!--   darker variant of colorPrimary (for status bar, contextual app bars) -->
        <!--导航条的颜色设置-->
        <item name="android:colorPrimaryDark">@color/start_color</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">@color/start_color</item>
        <!--状态条的颜色设置-->
        <item name="android:statusBarColor">@color/start_color</item>
    </style>


</resources>