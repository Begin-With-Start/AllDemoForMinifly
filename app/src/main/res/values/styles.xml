<resources>

    <!-- Base application theme. -->
    <style name="harf_transparents">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>
    </style>

    <!--做了沉浸式的状态栏之后，为了5.0一下的进行的定制.都没有给actionbar的设置-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:actionBarTheme">@style/Theme.AppCompat.DayNight.NoActionBar</item>
    </style>

    <style name="DeepTheme" parent="AppTheme">

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <!--做了沉浸式的状态栏之后，为了5.0一下的进行的定制.-->
</resources>
